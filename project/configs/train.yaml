# ========================================
# 训练配置
# ========================================
# 面向光纤路径序列 -> 基函数系数回归的完整训练配置。

seed: 42

# 设备: "auto" | "cpu" | "cuda"
device: "auto"

# 数据配置
data:
  manifest: "data/processed2/manifest.json"
  samples_dir: "data/processed2"
  use_dataframe: false

# Dataset stats path (global std for kappa/w)
stats_path: "data/processed/stats.json"

# 模型配置
model:
  in_features: 5
  d_model: 128
  n_layers: 4
  dropout: 0.0
  out_coeffs: 16
  encoder_type: "auto"

# 基函数配置
basis:
  M: 4
  N: 4
  Lx: 0.3
  Ly: 1.0

# 训练超参数
train:
  batch_size: 8
  epochs: 100
  lr: 0.001
  weight_decay: 0.0
  grad_clip: 1.0

# 损失权重
loss:
  huber_delta: 1.0
  lambda_kappa: 1.0
  lambda_hf: 0.0
  lambda_w: 1.0
  lambda_bc: 0.0

# Curriculum config (optional)
curriculum:
  enabled: false
  stageA_steps: 0
  stageB_warmup_steps: 0

# 运行输出
run:
  save_dir: "runs"
  name: ""
